package com.alinesno.infra.smart.assistant.scene.common.utils;

import java.io.*;

public class WordToPdfConverter {

    public static void convertToPdf(File inputWord, File outputFile) throws IOException {
//        try (InputStream docxInputStream = new FileInputStream(inputWord);
//             OutputStream outputStream = new FileOutputStream(outputFile)) {
//
//            XWPFDocument document = new XWPFDocument(docxInputStream);
//            PdfOptions options = PdfOptions.create();
//            PdfConverter.getInstance().convert(document, outputStream, options);
//        }
    }
}